events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    server {
        listen 8081;
        server_name _;
        root /usr/share/nginx/html;

        autoindex off;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
